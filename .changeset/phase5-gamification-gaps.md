---
"@heybray/gamification": minor
"@heybray/taxonomy": patch
---

Add `GamificationService.setRewardTiers` and `ensureDefaultRewardTiers`, accept mastery dimension slug on leaderboard scope queries (back-compat with `category`), and remove misleading `content_type` schema defaults.
