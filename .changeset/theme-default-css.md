---
"@heybray/ui": minor
---

Add `theme-default.css` — shared Bray pink baseline CSS variables (nav chrome, gamification tokens, shadcn semantic palette). Apps import via `@import "@heybray/ui/theme-default.css"` and override locally.
