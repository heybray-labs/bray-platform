---
"@heybray/media": minor
"@heybray/react": patch
---

Add neutral `detachedCount` to media delete response; keep `detachedFromScenarios` as a deprecated alias until 2.0.0. `@heybray/react` `MediaManagementPanel` reads `detachedCount` first.
